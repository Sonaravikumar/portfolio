export interface Profile {
  name: string;
  role: string;
  tagline: string;
  github: string;
  linkedin: string;
  about: string;
}

export interface SkillCategory {
  title: string;
  skills: string[];
}